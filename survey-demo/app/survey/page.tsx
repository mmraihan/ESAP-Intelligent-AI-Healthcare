import Survey from "@/components/Survey";

export const metadata = {
  title: "Healthcare Survey — ESAP",
  description: "Healthcare experience survey with AI analysis and doctor review pipeline",
};

export default function SurveyPage() {
  return <Survey />;
}
